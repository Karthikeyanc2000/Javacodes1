public class VeryAdvCalc extends AdvCalc {
    public double power(int x, int y)
    {
        return Math.pow(x,y);
        // always pow methode will return double so return value should be double.
    }

}
